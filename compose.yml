version: "3"
services:
  bot:
    container_name: art-bot-helper
    command: python main.py
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./:/app
    env_file:
      - stack.env
    restart: unless-stopped